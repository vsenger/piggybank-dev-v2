create table Category (
       id bigint not null,
        description varchar(255),
        primary key (id)
    );

insert into Category values (100, "Meals");
insert into Category values (420, "Others");
